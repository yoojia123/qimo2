// Placeholder JavaScript for toggling filters
document.querySelectorAll('.sidebar input[type="checkbox"]').forEach(checkbox => {
    checkbox.addEventListener('change', () => {
    console.log('Filter updated');
    // Add filter logic here
    });
    });